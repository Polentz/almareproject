name: field.blocks.gallery.name
icon: dashboard
preview: gallery
fields:
  images:
    label: field.blocks.gallery.images.label
    type: files
    query: page.images.filterBy('template', 'card')
    multiple: true
    layout: cards
    empty: field.blocks.gallery.images.empty
    uploads: card
    template: card
    link: true
    image:
      back: "#000"
      icon: false
      ratio: 1/1
      cover: true